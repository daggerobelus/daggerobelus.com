# Regenerable run byproducts — blind transcriptions and baseline replicates.
# The valuable artifacts (candidate method.md, configs, workflow, STATUS) are kept.
*/rounds/**/hyp/
*/rounds/**/baseline/
run-outputs/
